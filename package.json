{
  "name": "@nexys/utils",
  "version": "3.7.6",
  "main": "dist/index.js",
  "dependencies": {
    "@babel/runtime": "^7.5.5"
  },
  "scripts": {
    "test": "rimraf dist/ && jest ",
    "build": "rimraf dist/ && babel src/ --out-dir dist/ --extensions \".js,.ts\";rm dist/*.test.js",
    "buildTs": "tsc --init --declaration --allowSyntheticDefaultImports --target esnext --outDir dist/"
  },
  "files": [
    "dist/*"
  ],
  "devDependencies": {
    "@nexys/babelsetup": "^1.0.3"
  }
}
