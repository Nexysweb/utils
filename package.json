{
  "name": "@nexys/utils",
  "version": "3.5.10",
  "main": "dist/index.js",
  "dependencies": {
    "@babel/runtime": "^7.5.5"
  },
  "scripts": {
    "test": "rimraf dist/ && jest ",
    "build": "rimraf dist/ && ./node_modules/.bin/babel src/ --out-dir dist/;rm dist/*.test.js"
  },
  "files": [
    "dist/*"
  ],
  "devDependencies": {
    "@babel/cli": "^7.5.5",
    "@babel/core": "^7.5.5",
    "@babel/plugin-transform-runtime": "^7.5.5",
    "@babel/preset-env": "^7.5.5",
    "babel-jest": "^24.8.0",
    "babel-preset-minify": "^0.5.1",
    "jest": "^24.9.0",
    "rimraf": "^3.0.0"
  }
}
