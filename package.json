{
  "name": "@nexys/utils",
  "version": "3.7.7",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "dependencies": {
    "@babel/runtime": "^7.5.5"
  },
  "scripts": {
    "test": "rimraf dist/ && jest ",
    "builddep": "rimraf dist/ && babel src/ --out-dir dist/ --extensions \".js,.ts\";rm dist/*.test.js",
    "build": "tsc --allowSyntheticDefaultImports --target esnext --outDir dist/"
  },
  "files": [
    "dist/*"
  ],
  "devDependencies": {
    "@nexys/babelsetup": "^1.0.3"
  }
}
